name: "Pr Opened"
on:
  push:
    branches:
      - "master"
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Semantic Release Action
        uses: codfish/semantic-release-action@v1
