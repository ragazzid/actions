name: "Pr Opened"
on:
  pull_request:
    branches:
      - "master"
jobs:
  test:
    runs-on: ubuntu-latest
    if: github.ref == 'diego'
    steps:
      - uses: actions/checkout@v1
      - name: Test
        run: date